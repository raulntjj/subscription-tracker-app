import { LoginContainer } from "@/modules/auth/components/containers/login-container";

export const metadata = {
  title: "Sign In - SubTracker",
  description: "Sign in to your SubTracker account.",
};

export default function LoginPage() {
  return <LoginContainer />;
}
